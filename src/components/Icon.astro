---
interface Props {
  iconSize?:14|16;
  iconName?:IconName;
  iconColor?:string;
}

const { iconSize = 14, iconName, iconColor } = Astro.props;

import { Icon as IconType, ChevronDown, ExternalLink, Link, Menu, Undo2 } from "@lucide/astro";

let IconComponent: typeof IconType = ChevronDown;

---

<IconComponent color="white" size={iconSize}/>